<template>
  //<router-link class="app-link" :to="{ name: 'language', params: { country: country.code } }">
    <div class="app-card -shadow">
      <img v-bind:src="country.image" class="flag">
      <div class="country">
        <span class="bold">{{country.name}}</span>
        <br />
        {{country.english}}
      </div>
    </div>
//  </router-link>
</template>

<script>
export default {
  props: {
    country: Object
  }
}
</script>

<style scoped>
img.flag {
  width: 25%;
}
div.country {
  vertical-align: top;
  width: 70%;
  font-size: 24px;
  float: right;
}
.country {
  text-align: left;
}
div.break {
  display: inline;
}
.bold {
  font-weight: bold;
}
</style>


script>
export default {
  props: {
    country: Object
  },
  methods: {
    updateBookmark1: function(bookmark) {
      console.log ('Updatng')
      this.$store
        .dispatch('updateBookmark', this.bookmark)
        .then(() => {
          this.$router.push({
            name: 'languages',
            params: { country: this.bookmark.country.id }
          })
        })
        .catch(() => {
          console.log('There was a problem storing country')
        })
    },
  }
}
